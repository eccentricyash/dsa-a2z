# Mistake Log

Format: **Problem** → what I missed → root cause (pattern recognition / invariant / edge case / complexity / implementation / misread) → fix → revisit day

- **Primality drill (91)** [D1] → declared 91 prime without testing divisors; 91 = 7×13 → root cause: implementation habit — eyeballed instead of running the cheap check → fix: when a check costs seconds, run it; never "vibe" arithmetic → revisit: warm-up around D4 with another looks-prime number (e.g. 119 = 7×17, 133 = 7×19)
- **Modulo semantics (gcd drill)** [D1] → claimed 7 % 21 = 0; also invented a "take the smaller number as gcd" rule from the wrong result → root cause: implementation — misconception that a % b is 0 when a < b (it's a, unchanged) → fix: a % b = what's left after removing whole b's from a; when a < b nothing is removed → revisit: warm-up D3–4, quiz small%big cases
- **Euclid invariant (gcd drill)** [D1] → asked WHY gcd(a,b)=gcd(b,a%b) is safe; described the mechanics (remainder shrinks, pair swaps) instead of the invariant (common-divisor set is preserved) → root cause: invariant — first exposure to the concept → fix: taught explicitly; must reproduce "same common divisors at every step" unprompted → revisit: D2 warm-up
- **LC #7 Reverse Integer** [D2] → (a) runtime error: computed r*10 to CHECK overflow — the check itself overflows; (b) skipped the statement's overflow clause entirely → root causes: misread + new concept (pre-emptive overflow guard via division: r > INT_MAX/10 / r < INT_MIN/10) → fix: taught; guard BEFORE the multiply → revisit: D4 warm-up (ask for the guard from memory)
- **LC #7, second bug** [D2] → wrote `r > INT_MIN/10` — wrong inequality direction on the floor bound (fired on x=123) → root cause: implementation — boundary direction; didn't hand-trace before submitting → fix: ceiling→`>`, floor→`<`; ALWAYS trace smallest failing input by hand → revisit: recurring-tendency watch, note in basics arsenal
- **LC #9 Palindrome Number** [D2] → (a) proposed "make negative positive then check" — missed that the number is judged as TEXT and '-' has a position; every negative is instantly false; (b) reported "solved" while a case was failing → root causes: pattern recognition (quantity-vs-text framing was new) + reporting accuracy → fix: taught the two-worlds rule (sign rides along in arithmetic; sign has a position in text) → revisit: D4 warm-up
- **LC #9, second submit** [D2] → runtime error on 1234567899: left `int r` despite explicit instruction to widen to long long → root cause: ⚠️ RECURRING TENDENCY — skips parts of instructions/statements (Q3 invariant skipped, #7 overflow clause skipped, #9 teaser skipped twice, this fix skipped) → fix: before every submit, re-read the statement + my last message and tick off each requirement → revisit: watch every problem; call out each occurrence
- **Print Divisors** [D2] → (a) `push_back(n/i)` placed outside `if(n%i==0)` — brace scope bug, pushed n/i for every i; (b) `i*i<n` instead of `<=` — misses √n divisor on perfect squares → root causes: implementation (brace scope) + edge case (boundary) → fix: found both via evidence-reading (traced impossible output value 23) → revisit: D5 warm-up
- **Check Prime** [D2] → same `i*i<n` boundary bug AGAIN, minutes after fixing it in Divisors — n=4 loop never runs → true → root cause: ⚠️ SECOND RECURRING TENDENCY — boundary conditions (`<` vs `<=`, `>` vs `<`): 3 boundary bugs today (INT_MIN direction, divisors <=, prime <=) → fix: ritual — at every loop/if boundary ask "what happens AT the boundary value exactly?" and test a perfect square / the exact limit → revisit: every warm-up until it stops
